1) Initialized the folder into a project folder (uv init .)
2) Created a virtual environment (uv venv)
3) Created template.py file, wrote the code for creating the multiple files in the project folder.
4) In "src/cnnClassifier/constants/__init__.py" file location, I wrote the code there.
5) Then in the "src/cnnClassifier/__init__.py" file, I wrote another code there also.
6) Then, in the main.py file, I am importing the logger from cnnClassifier folder.